{
  "code": "EACCES",
  "title": "Permission denied",
  "description": "The process attempted to access a file or resource without proper permissions.",
  "steps": [
    "Check file/directory permissions with `ls -l`.",
    "Adjust permissions/ownership using `chmod` / `chown`.",
    "Confirm the user/group has access; use `sudo` only if appropriate."
  ],
  "image": "",
  "resources": [
    { "label": "GNU C Library: Error Codes", "url": "https://www.gnu.org/software/libc/manual/html_node/Error-Codes.html" }
  ]
}
